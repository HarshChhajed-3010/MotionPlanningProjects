cd Onedrive, Dekstop, WPI, MotionPlanning

git add .
git commit -m "update message"
git push -u origin main



##############
in case Lecture PDFs/ get uploaded how to remove it? 

git rm -r --cached "Lecture PDFs/"
git commit -m "Removed Lecture PDFs/"
git push -u origin main

